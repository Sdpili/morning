name: morning
on:
  schedule:
  - cron:  '0 21 * * *'
  workflow_dispatch:

jobs:
  send_message:
    runs-on: ubuntu-latest
    name: send morning to your girlfriend

    steps:
    - name: checkout
      uses: actions/checkout@v3
      with:
        ref: master

    - name: sender
      uses: actions/setup-python@v2
      with:
        python-version: '3.x'
        architecture: 'x64'
    - run: pip install -r ./requirements.txt && python ./main.py

    env:
      APP_ID: ${{ secrets.APP_ID_CS }}
      APP_SECRET: ${{ secrets.APP_SECRET_CS }}
      TEMPLATE_ID: ${{ secrets.TEMPLATE_ID_CS }}
      USER_ID: ${{ secrets.USER_ID_CS }}
      START_DATE: ${{ secrets.START_DATE_CS }}
      BIRTHDAY: ${{ secrets.BIRTHDAY_CS }}
      CITY: ${{ secrets.CITY_CS }}
